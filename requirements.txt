# GPU path: full-resolution PyTorch pipeline under src/ and gpu_experiments/.
# For the lightweight CPU sample-efficiency sweeps in cpu_experiments/, use
# cpu_experiments/requirements.txt instead — it has no torch dependency and
# installs in seconds.
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
pyyaml>=6.0
torch>=2.2
torchvision>=0.17
tqdm>=4.66
